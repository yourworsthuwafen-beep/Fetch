If you want to add this as a shortcut do "echo "alias shortcut='python ~/your-path-to-fetch'" >> ~/.bashrc && source ~/.bashrc" make sure to replace shortcut with your shortcut name and ~/your-path-to-fetch with your real path and then do ". ~/.bashrc"
